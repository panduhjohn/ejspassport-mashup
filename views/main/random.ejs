<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head') %>
        <title>Random Users</title>
    </head>
    <body>
        <header>Random Users</header>
        <% sortUsers.forEach(({name: {first, last}, picture: {large}}) => { %>
            <div class="randomContainer">
                <img class="random" src="<%= `${large}` %>" />
                <p><%= `${first} ${last}` %></p>
            </div>
        <% }) %> <%- include('../partials/footer') %>
    </body>
</html>
